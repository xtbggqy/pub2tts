# PubMed文献一站式处理系统依赖库

# HTTP和API客户端 - 支持语言模型和TTS功能
openai>=1.0.0          # 支持通义千问等API调用
dashscope>=1.13.0      # 阿里云语音合成和语言模型支持
requests>=2.31.0       # HTTP请求库
python-dotenv>=1.0.0   # 环境变量管理

# 文献检索和数据处理
biopython>=1.81        # Pubmed文献访问接口
beautifulsoup4>=4.12.0 # HTML解析
PyPDF2>=3.0.0          # PDF文件操作

# 进度显示与用户界面
tqdm>=4.66.0           # 进度条显示
colorama>=0.4.6        # Windows终端颜色支持

# Token计数与性能优化
tiktoken>=0.5.0        # 计算token数量，用于成本估算

# 数据可视化
matplotlib>=3.5.0      # 图表绘制
numpy>=1.20.0          # 数值计算

# 可选依赖项（如果需要特定功能）
# pandas>=1.3.0        # 如需高级数据分析，取消此行注释
# seaborn>=0.11.0      # 如需更美观的统计图表，取消此行注释
